// TEMP diagnostic probe — removed after diagnosis. No tests live here anymore.
// (Diagnosis summary: page scroll locked by toc.js revealActiveLink() scrolling
// the active header link into view; keyboard tab order broken by app JS init.)